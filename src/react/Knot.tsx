import React from "react";
import {ReactElement} from "react";

export interface KnotProps {

}

export function Knot(props: KnotProps): ReactElement {
    return (
        <h1 className="text-5xl font-bold">Knot</h1>
    );
}